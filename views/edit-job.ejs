<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Job</title>
  <link rel="stylesheet" href="/edit-job.css" />
</head>
<body>
  <div class="container">
    <h2>Edit Job Posting</h2>

    <form method="POST" action="/edit-job/<%= job._id %>">
      <input name="title" value="<%= job.title %>" placeholder="Job Title" required />
      <input name="company" value="<%= job.company %>" placeholder="Company" required />
      <input name="location" value="<%= job.location %>" placeholder="Location" required />
      <input name="type" value="<%= job.type %>" placeholder="Job Type" required />
      <textarea name="description" placeholder="Description" required><%= job.description %></textarea>
      <input name="applyLink" value="<%= job.applyLink %>" placeholder="Apply Link" required />
      <button type="submit">Update Job</button>
    </form>

    <div class="nav-links">
      <a href="/manage-jobs">Back to Manage Jobs</a>
    </div>
  </div>
</body>
</html>
